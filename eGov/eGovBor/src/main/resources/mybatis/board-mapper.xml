<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Board">
	
	<select id="totalRow" resultType="int">
		select count(*) from board where title like concat('%',#{search},'%')<if test="!category.equals('선택')"> and category = #{category}</if>
	</select>
	
	<select id="getBoardList" resultType="com.toy.dto.BoardDTO">
		SELECT *
		FROM board
		WHERE title 
		LIKE concat('%',#{search},'%')<if test="!category.equals('선택')"> and category = #{category} </if>
		order by seq desc
		limit ${getRow} offset ${whereRow}
	</select>
	
</mapper>